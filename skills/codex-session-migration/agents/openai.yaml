interface:
  display_name: "Codex Session Migration"
  short_description: "Repair, move, or rebind Codex threads safely"
  default_prompt: "Use $codex-session-migration to repair, rebind, or migrate Codex threads across homes or workspace paths."
policy:
  allow_implicit_invocation: true
