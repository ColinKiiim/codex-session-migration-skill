interface:
  display_name: "Codex Session Migration"
  short_description: "Move, import, rebind, or recover Codex threads"
  default_prompt: "Use $codex-session-migration to migrate, import from alternate local Codex homes such as Antigravity instances, rebind, repair, clone, diagnose malformed session files, recover hidden Codex threads, or fix workspace rename/path-prefix drift across homes, workspace paths, and sidebar metadata."
policy:
  allow_implicit_invocation: true
